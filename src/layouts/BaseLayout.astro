---
import Footer from '@/components/Footer.astro'
import Header from '@/components/Header.astro'
const { title, description } = Astro.props
---

<style is:global>
  /*------ Variables ------*/
  :root {
    /*------ Colors ------*/
    --color-primary: #83f56e;
    --color-secondary: #f56ded;
    --color-tertiary: #7171f8;
    --color-white: #f2f2f2;
    --color-black: #2f2f2f;
    --color-grey: #494949;
    --color-light: #d8d8d8;
    /*------ Fonts ------*/
    --font-family-body: 'Lexend', sans-serif;
    --font-family-headings: 'Permanent Marker', cursive;
    --font-headline1: 400 48px/62.4px var(--font-family-headings);
    --font-headline2: 400 36px/46.8px var(--font-family-headings);
    --font-headline3: 500 24px/31.2px var(--font-family-headings);
    --font-headline1-back: 400 48px/62.4px var(--font-family-body);
    --font-headline2-back: 400 36px/46.8px var(--font-family-body);
    --font-headline3-back: 500 24px/31.2px var(--font-family-body);
    --font-headline4-back: 500 18px/23.4px var(--font-family-body);
    --font-body: 500 18px/28px var(--font-family-body);
    --font-body-small: 400 18px/23.4px var(--font-family-body);
    --font-labels: 400 18px/23.4px var(--font-family);

    --font-button: 500 18px/23.4px var(--font-family);
    --font-link: 400 16px/20.8px var(--font-family);
    --font-link-menu: 300 18px/23.4px var(--font-family);
    /*------ Numbers ------*/
    --font-letter-spacing: 0.08em;
  }

  * {
    margin: 0;
  }
  body {
    font: var(--font-body);
    background: var(--color-black);
    color: var(--color-light);
    overflow-x: hidden;
  }

  h1 {
    font: var(--font-headline1);
    text-transform: uppercase;
  }

  h2 {
    font: var(--font-headline2);
  }

  h3 {
    font: var(--font-headline3);
  }

  h4 {
    font: var(--font-headline4);
  }

  h5 {
    font: var(--font-headline5);
    font-style: italic;
  }

  a {
    font: var(--font-link);
    text-decoration: none;
  }

  section {
    padding-inline-start: 20px;
    padding-inline-end: 20px;
  }

  .container {
    max-inline-size: 1158px;
    margin: 0 auto;
  }

  .section__title {
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding-left: 21px;
    border-left: 3px solid var(--color-link);
  }

  .section__description {
    margin-top: 18px;
    margin-bottom: 25px;
  }

  @media only screen and (min-width: 560px) {
    .section__description {
      max-width: 500px;
      margin-bottom: 50px;
      margin-left: 28px;
    }
  }

  @media only screen and (min-width: 1024px) {
    section {
      padding-bottom: 80px;
    }
  }

  .btn {
    display: flex;
    justify-content: center;
    font: var(--font-button);
    border-radius: 12px;
    padding-block-start: 8px;
    padding-block-end: 8px;
    padding-inline-start: 32px;
    padding-inline-end: 32px;
    margin-block-start: 12px;
    margin-block-end: 12px;
    text-decoration: none;
    cursor: pointer;
    background-color: var(--color-vanilla);
    border: 1px solid var(--color-secondary);
    color: var(--color-secondary);
  }

  .btn img {
    margin-right: 10px;
  }

  .btn-primary {
    background-color: var(--color-white);
    border-color: var(--color-link);
    color: var(--color-primary);
  }

  .btn-primary:hover {
    background-color: var(--color-link);
    color: var(--color-white);
  }

  .btn-secondary {
    border-color: var(--color-vanilla);
    color: var(--color-secondary);
  }

  .btn-secondary:hover {
    border: 1px solid var(--color-primary);
    color: var(--color-primary);
  }
</style>

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.png" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <meta property="og:description" content={description} />
    <meta property="twitter:description" content={description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lexend:wght@100..900&family=Permanent+Marker&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>

    <meta property="og:image" content="" />
    <meta property="twitter:image" content="" />
    <meta property="og:title" content={title} />
    <meta property="twitter:title" content={title} />
    <meta property="og:url" content="" />
    <meta property="twitter:url" content="" />
    <meta property="og:type" content="website" />
    <meta property="twitter:card" content="summary_large_image" />
  </head>
  <body>
    <Header />
    <slot />
    <Footer />
  </body>
</html>
